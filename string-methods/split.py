# Método split string: Divide una variable según el caracter consultado por el cual separar. Una vez separado, retorna
# un arreglo con las palabras ya individuales.

texto = "Ejemplo, para, usar, el, split."
print(texto.split(" "))
print(texto.split(", "))