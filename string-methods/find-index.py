# Método find/index string: Retorna la posición de la primera palabra similar a la consulta dentro del
# paréntesis y comillas dobles.

arreglo = "Este es un ejemplo del uso de find que es uno de los varios métodos string."
print(arreglo.find("es"))
print(arreglo.index("es"))